#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, j, k;

    printf("Enter an integer : ");
    scanf("%d", &i);
    printf("Enter an integer : ");
    scanf("%d", &j);

    k = i == j;

    printf("%d == %d yields %d\n", i, j, k);

    return 0;
}
