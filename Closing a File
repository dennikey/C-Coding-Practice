#include <stdio.h>

int main (void)
{
    FILE *fp = NULL;

    fp = fopen("alpha.txt", "w");
    if (fp != NULL) {
        fclose(fp);
    }
    else {
        printf("Failed to open\n");
    }
    return 0;
}
